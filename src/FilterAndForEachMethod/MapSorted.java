package FilterAndForEachMethod;

import java.util.*;

public class MapSorted {
    Map<Integer,String> map=new HashMap<>();
//
//        map.put(1,"d");
//        map.put(2,"a");
//        map.put(3,"f");
//        map.put(4,"g");
//    List<Map.Entry<Integer,String>> ent=new ArrayList<>(map.entrySet());
//
//Collections.sort(ent,(o1,o2)->o1.getKey().compareTo(o2.getKey()));
//
//map.entrySet().stream().sorted(Map.Entry.comparingByKey()).forEach(t-> System.out.println(t));
//
//        map.entrySet().stream().map(e->e.getValue()).sorted().forEach(e-> System.out.println(e));
//
////******************
//
//Map<emp,Integer> any=new HashMap<>();
//
//
//any.entrySet().stream().sorted(Map.Entry.comparingByKey(Comparator.comparing(emp::methodname).reversed())).forEach();
//

}



